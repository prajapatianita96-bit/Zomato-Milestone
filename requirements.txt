# ─────────────────────────────────────────────
# Zomato AI Restaurant Recommendation System
# requirements.txt
# ─────────────────────────────────────────────

# ── Data ──────────────────────────────────────
datasets>=2.18.0          # Hugging Face dataset loader
pandas>=2.0.0             # DataFrame operations
numpy>=1.26.0             # Numerical utilities

# ── LLM Integration ───────────────────────────
groq>=0.5.0                  # Groq API SDK

# ── Config & Environment ──────────────────────
python-dotenv>=1.0.0      # Load .env variables
PyYAML>=6.0               # Parse config.yaml

# ── Fuzzy Matching (InputHandler) ─────────────
thefuzz>=0.22.1           # Fuzzy string matching for location/cuisine
python-Levenshtein>=0.25.0  # Speed up thefuzz

# ── File Locking (Cache Safety) ───────────────
portalocker>=2.8.2        # Safe concurrent cache writes

# ── Testing ───────────────────────────────────
pytest>=8.0.0             # Unit & integration testing
pytest-cov>=5.0.0         # Code coverage reporting

# ── API & Backend ─────────────────────────────
fastapi>=0.100.0          # Web framework
uvicorn>=0.23.0           # ASGI server
pydantic>=2.0.0           # Data validation
streamlit>=1.30.0         # Streamlit UI framework
